---
import Layout from '../layouts/Layout.astro';
---

<Layout 
  title="Stránka nenalezena | Lagoon Masáže Mladá Boleslav"
  description="Omlouváme se, ale tato stránka neexistuje. Vraťte se zpět na hlavní stranu salonu Lagoon Masáže a objevte naše relaxační procedury."
  noindex={true}
  forceScrolledNavbar={true}
>
  <section class="error-page">
    <div class="error-page__container">
      <span class="error-page__eyebrow">404</span>
      <h1 class="error-page__title">Tady se čas zastavil<br/>až příliš</h1>
      <p class="error-page__text">
        Omlouváme se, ale stránka, kterou hledáte, neexistuje, nebo byla přesunuta. 
        Pojďme vás vrátit zpět tam, kde to znáte.
      </p>
      <a href="/" class="error-page__cta">Zpět na hlavní stranu</a>
    </div>
  </section>
</Layout>

<style>
  .error-page {
    min-height: 100dvh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-brown);
    padding: 120px 1.25rem 4rem;
    text-align: center;
  }
  .error-page__container {
    max-width: 600px;
    margin: 0 auto;
    animation: fadeUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }
  .error-page__eyebrow {
    display: block;
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--color-gold);
    margin-bottom: 1.5rem;
  }
  .error-page__title {
    font-family: var(--font-display);
    font-size: clamp(2.8rem, 6vw, 4.5rem);
    font-weight: 300;
    font-style: normal;
    line-height: 1.05;
    color: #ffffff;
    margin-bottom: 1.5rem;
  }
  .error-page__text {
    font-family: var(--font-body);
    font-size: clamp(1rem, 2vw, 1.15rem);
    color: rgba(255, 255, 255, 0.55);
    line-height: 1.65;
    margin: 0 auto 2.5rem;
    max-width: 420px;
  }
  .error-page__cta {
    font-family: var(--font-display);
    font-size: 1.25rem;
    font-weight: 400;
    font-style: normal;
    letter-spacing: 0.03em;
    text-decoration: none;
    color: #ffffff;
    background: var(--color-btn);
    padding: 0.75rem 2.5rem;
    border-radius: 999px;
    display: inline-flex;
    align-items: center;
    transition: background 0.3s ease;
  }
  @media (hover: hover) {
    .error-page__cta:hover { background: var(--color-btn-hover); }
  }
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @media (prefers-reduced-motion: reduce) {
    .error-page__container { animation: none; }
    .error-page__cta { transition: none !important; transform: none !important; }
  }
</style>